// Placeholder so the protected route exists; the real brand list ships in Phase 3.
export default function BrandsPage() {
  return (
    <div>
      <p className="font-mono text-[11px] uppercase tracking-widest text-text-muted">
        <span className="text-accent">•</span> Brands
      </p>
      <h1 className="mt-2 font-display text-4xl text-text">Your brands</h1>
      <p className="mt-2 text-sm text-text-muted">Brand management arrives in Phase 3.</p>
    </div>
  )
}
